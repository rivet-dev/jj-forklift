// Integration-test home for pure jj stack parsing/formatting helpers.
//
// Keep tests here instead of inline `src/` test modules. Tests that need real
// jj behavior should use `tests/common/mod.rs`.
